triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_regular_empire = yes
			is_mem_runesmith = yes
		}
		NOR = {
			has_designation = col_bureau
			has_designation = col_factory
			has_designation = col_foundry
			has_designation = col_ecu_factory
			has_designation = col_ecu_foundry
			has_designation = col_capital_foundry 
			has_designation = col_capital_factory
			
		}
	}
	modifier = {
		job_mem_ancestors_grudge_relicsmith_per_pop = 0.05
		job_mem_ancestors_grudge_runesmith_per_pop = 0.05
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = yes
			is_mem_runesmith = yes
		}
		NOR = {
			has_designation = col_bureau
			has_designation = col_factory
			has_designation = col_foundry
			has_designation = col_ecu_factory
			has_designation = col_ecu_foundry
			has_designation = col_capital_foundry 
			has_designation = col_capital_factory
		}
	}
	modifier = {
		job_mem_ancestors_grudge_relicsmith_drone_per_pop = 0.05
		job_mem_ancestors_grudge_runesmith_drone_per_pop = 0.05
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_regular_empire = yes
			is_mem_runesmith = yes
		}
		OR = {
			has_designation = col_bureau
			has_designation = col_factory
			has_designation = col_ecu_factory			
			has_designation = col_capital_factory
		}
	}
	modifier = {
		job_mem_ancestors_grudge_runesmith_per_pop = 0.1
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = yes
			is_mem_runesmith = yes
		}
		OR = {
			has_designation = col_bureau
			has_designation = col_factory
			has_designation = col_ecu_factory
			has_designation = col_capital_factory
		}
	}
	modifier = {
		job_mem_ancestors_grudge_runesmith_drone_per_pop = 0.1
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_regular_empire = yes
			is_mem_runesmith = yes
		}
		OR = {
			has_designation = col_foundry
			has_designation = col_ecu_foundry
			has_designation = col_capital_foundry 
		}
	}
	modifier = {
		job_mem_ancestors_grudge_relicsmith_per_pop = 0.1
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = yes
			is_mem_runesmith = yes
		}
		OR = {
			has_designation = col_foundry
			has_designation = col_ecu_foundry
			has_designation = col_capital_foundry 
		}
	}
	modifier = {
		job_mem_ancestors_grudge_relicsmith_drone_per_pop = 0.1
	}
}

triggered_desc = {
	trigger = {
		exists = owner
		owner = {
			is_regular_empire = yes
			is_mem_runesmith = yes
		}
	}
	text = mem_ancestors_grudge_building_forge_effect_designation
}
