mem_ancestors_grudge_situation_speed = {
	base = -100
	#add = modifier:mem_ancestors_grudge_artifact_power_produces_mult
	mult = modifier:mem_ancestors_grudge_artifact_power_produces_mult
}

mem_ancestors_grudge_situation_speed_runes = {
	base = 100
	#add = modifier:mem_ancestors_grudge_artifact_power_produces_mult
	mult = modifier:mem_ancestors_grudge_rune_power_produces_mult
}

# pop number but /100
mem_ancestors_grudge_situation_pop_count = {
	base = 0
	add = trigger:num_pops
	mult = 0.1
}

# negativ growth
mem_ancestors_grudge_factory_pop_count = {
	base = 0
	add = trigger:num_pops
	max = 90
}

# determines rarity of relics
mem_ancestors_grudge_situation_rarity = {
	base = 0
	add = modifier:mem_ancestors_grudge_artifact_power_produces_mult	# actual modifier
	mult = 100	# go to full numbers to round down, and make compare
	divide = 15	# every 15
	#floor = yes #round down
	#add = 1 # temporary fix, pls dont break anything # otheriwse it would need 25% more than for all rarities
	max = 7.9 # so higher than that will be cut off
}

# determines rarity of relics
mem_ancestors_grudge_situation_rarity_floored = {
	base = 0
	add = modifier:mem_ancestors_grudge_artifact_power_produces_mult	# actual modifier
	mult = 100	# go to full numbers to round down, and make compare
	divide = 15	# every 25
	floor = yes #round down
	#add = 1 # temporary fix, pls dont break anything # otheriwse it would need 25% more than for all rarities
	max = 7
}

# determines rarity of relics, tooltip
mem_ancestors_grudge_situation_rarity_tooltip = {
	base = 0
	add = value:mem_ancestors_grudge_situation_rarity
	add = mem_ancestors_grudge_rarity_increase
	max = 7.9 # so higher than that will be cut off
}


# determines purity of relics, tooltip
mem_ancestors_grudge_situation_purity_tooltip = {
	base = 1
	add = modifier:mem_ancestors_grudge_rune_power_produces_mult
	mult = 12.5 # weight of postive modifers
	subtract = value:mem_ancestors_grudge_situation_rarity_tooltip 	
}

# overflow
mem_ancestors_grudge_hoarder_overflow = {
	base = 1
	complex_trigger_modifier = {
		trigger = resource_revenue_compare
		parameters = {
			resource = minor_artifacts
		}
		mode = mult
	}
	#max = 4
}

# relics
mem_ancestors_grudge_hoarder_building = {
	base = 1
	add = trigger:num_owned_relics
	#max = 4
}

# extra forge jobs
mem_ancestors_grudge_relicforge_jobs = {
	base = 2
	add = this.mem_ancestors_grudge_relicforge_extrajobs
	#max = 4
}


mem_smith_job_number = {
	base = 0
	complex_trigger_modifier = {
		trigger = count_owned_pop
		parameters = {
			limit = {
				OR = {
					has_job = mem_ancestors_grudge_relicsmith
					has_job = mem_ancestors_grudge_relicsmith_drone
					has_job = mem_ancestors_grudge_runesmith
					has_job = mem_ancestors_grudge_runesmith_drone
				}
			}
		}
		mult = 0.3
		mode = add
	}
	max = 20
	# complex_trigger_modifier = {
	# 	potential = {
	# 		owner = {
	# 			is_overlord = yes
	# 		}
	# 	}
	# 	trigger = count_galaxy_planet
	# 	parameters = {
	# 		limit = {
	# 			has_owner = yes
	# 			has_holding = {
	# 				holding = holding_knight_commandery
	# 				owner = root
	# 			}
	# 		}
	# 	}
	# 	mult = 0.05
	# 	mode = add
	# }
	# complex_trigger_modifier = {
	# 	potential = {
	# 		owner = {
	# 			has_corporate_government = yes
	# 		}
	# 	}
	# 	trigger = count_galaxy_planet
	# 	parameters = {
	# 		limit = {
	# 			has_owner = yes
	# 			has_holding = {
	# 				holding = building_knightly_theme_park
	# 				owner = root
	# 			}
	# 		}
	# 	}
	# 	mult = 0.01
	# 	mode = add
	# }
}
